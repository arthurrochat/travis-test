language: node_js
node_js: 'node'

cache:
  directories:
    - node_modules

stages:
  - name: testing
    if: branch = master

deploy:
  provider: script
  script: bash scripts/deploy.sh
  on:
    tags: true

jobs:
  include:
    - stage: testing
      if: branch = master
      script: npm run test